class authservice {
  void signupuser({
    required string email,
    required stringfirstname,
    required string lastname, 
    required string numérotélephone,
    required string adresse,
    required string city,
    required string zip,
    required string password,
  }) async {
    try{
      user user =user(id:'',name:name,)

    }catch(error){
      avoid emty catch bloks.
    }
  }
}